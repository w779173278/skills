# [实体名称] (Entity Name) 实体定义

> [!NOTE]
> **文档类型**：Explanation (领域模型-[实体名])
> **目标读者**：系统分析师、核心研发、测试与运维人员。
> **文档目标**：规范定义[实体名]的各维度控制标准与运行时边界。

## 1. 定义
**[实体中文名称]** 是指 [一句话权威定义其业务本源定位]。

## 2. 模型关系
- **与 [关联实体A] 关系**：1 对多 (1 : N) / 多对多 (N : M)。[说明业务场景下的关联关系]。
- **与 [关联实体B] 关系**：[例如：父子层级嵌套，最大支持 N 层]。

## 3. 核心属性
对应物理表：`t_sys_[实体名]`（若无物理表，可直接写内存模型/DTO属性）
| 数据库字段 / 属性名 | 代码属性 / 变量名 | 数据类型 | 可空性 | 描述说明 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `id` | `Long / String` | N | 实体唯一主键 |
| `status` | `status` | `Integer / Enum`| N | 状态机，枚举取值：1-待激活，2-运行中，3-已失效 |

## 4. 行为约束
- **防越权原则**：[该实体在读写时的权限边界限制，例如：仅属主租户/角色可操作]。
- **状态流转约束**：[状态改变时的前置检查，例如：必须先通过 A 校验，才能从“待激活”变为“运行中”]。
- **生命周期机制**：[实体的创建与消亡，例如：是否支持物理删除？删除时是否级联清理下游关联的 XX 缓存？]。
